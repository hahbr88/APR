# AGENTS.md

이 문서는 이 저장소에서 작업하는 AI 에이전트와 개발자가 따라야 할 기본 규칙을 정의한다.

## 프로젝트 개요

- 카드사 이용내역을 통합하고 선택한 거래를 회사 양식의 지출결의서 XLSX로 내보내는 로컬 웹 애플리케이션이다.
- 삼성카드·신한카드 XLSX와 현대카드 HTML 형식 XLS를 지원한다.
- 원본 엑셀 템플릿은 `assets/payment-request-template.xlsx`이다.
- 사용자가 업로드한 카드 내역은 서버 디스크에 저장하지 않는다.
- 가맹점 분류와 초안만 `data` 디렉터리의 로컬 JSON 파일에 저장한다.

## 기술 구성

- 패키지 매니저는 반드시 `pnpm`을 사용한다.
- Node.js 20 이상과 TypeScript를 사용한다.
- 서버는 Express, 엑셀 처리는 ExcelJS, HTML 형식 XLS 처리는 Cheerio를 사용한다.
- 프런트엔드는 React, Vite, Tailwind CSS, shadcn/ui, TanStack Table, React Hook Form을 사용한다.
- 클라이언트와 서버의 요청 데이터는 `shared/schemas.ts`의 Zod 스키마로 검증한다.

## 주요 명령어

```bash
pnpm install
pnpm dev
pnpm typecheck
pnpm test
pnpm build
pnpm start
```

- 변경 후에는 최소한 `pnpm typecheck`를 실행한다.
- 파서, 스키마, 엑셀 생성 규칙을 변경하면 `pnpm test`를 실행한다.
- 프런트엔드를 변경하면 `pnpm build:client`, 서버를 변경하면 `pnpm build:server`를 실행한다.
- 기능 완료 전에는 가능하면 `pnpm build`까지 통과시킨다.

## 코드 작성 원칙

- 새 코드는 TypeScript로 작성하고 `strict` 타입 검사를 유지한다.
- 클라이언트 내부 경로는 가능한 경우 `@/` 별칭을 사용한다.
- 클라이언트와 서버가 함께 사용하는 타입·스키마·순수 함수는 `shared`에 둔다.
- 외부 입력을 신뢰하지 말고 API 경계에서 Zod로 검증한다.
- 카드사 파일은 파일명만으로 판별하지 말고 헤더와 내부 구조를 우선 사용한다.
- 정상적으로 제외해야 하는 합계·소계 행과 실제 파싱 오류 행을 구분한다.
- 오류 행을 조용히 누락하지 말고 사용자가 확인할 수 있도록 표시한다.
- 기존 사용자 변경과 무관한 파일은 수정하거나 정리하지 않는다.
- 파일 인코딩은 UTF-8을 유지하고 한글 문자열이 깨지지 않도록 확인한다.

## 지출결의서 생성 규칙

- 사용자 화면과 문서에서는 `결제요청서` 대신 `지출결의서`라는 명칭을 사용한다.
- 저장 파일명은 `지출결의서_{접수일 YYYYMMDD}_{접수자}.xlsx` 형식을 사용한다.
- `지출결의서` 시트는 선택 거래 전체를 거래일시가 최근인 순서로 기록한다.
- 기본 상세 영역 15건을 초과하면 같은 문서 안에 행을 추가한다.
- `경비사용내역서`에서 `링스테크내부`가 아닌 거래는 8행부터 최근순으로 기록한다.
- 일반 거래 영역이 부족하면 `기타` 소계 행 앞에 상세행을 추가한다.
- `링스테크내부` 거래는 `기타` 소계 아래의 내부 상세 영역에 최근순으로 기록한다.
- 내부 상세 영역이 부족하면 `링스테크 내부` 소계 행 앞에 상세행을 추가한다.
- 행 추가 시 병합, 테두리, 수식, 행 숨김 상태와 인쇄 영역을 함께 보정한다.
- 접수일, 합계, 소계 및 청구금액 수식이 Excel에서 정확히 표시되는지 검증한다.
- `AI영수증` 시트는 별도 요청이 없는 한 수정하지 않는다.

## 테스트 원칙

- 카드사 파서 변경 시 삼성·신한·현대 예제 파일을 모두 검증한다.
- 파서가 반환한 전체 거래가 미리보기 요청 스키마를 통과하는지 확인한다.
- 취소 거래, 음수 취소금액, 합계·소계 제외, 날짜 변환을 회귀 테스트에 포함한다.
- 15건 초과 및 경비사용내역서 영역 확장 시 실제 XLSX를 다시 읽어 값과 수식을 검증한다.
- 행 추가 로직을 변경하면 행의 `hidden` 속성과 테두리 스타일도 검사한다.

## 개인정보와 저장소 관리

- `assets/ex`의 실제 카드 이용내역은 Git에 추가하지 않는다.
- `data/*.json`의 사용자 분류 및 초안 데이터는 Git에 추가하지 않는다.
- 로그, 테스트 코드, 문서에 카드번호·승인번호·개인정보를 새로 복사하지 않는다.
- 비밀키, 토큰, 계정 정보 및 환경별 설정을 저장소에 커밋하지 않는다.

## Git 작업 규칙

- Git 커밋은 항상 사람이 직접 수행한다.
- AI 에이전트는 `git add`, `git commit`, `git commit --amend`, `git tag`, `git push`를 실행하지 않는다.
- 에이전트는 필요할 때 `git status`, `git diff`, `git log`로 변경 상태를 확인하고 커밋 대상과 메시지만 제안한다.
- 강제 푸시, 히스토리 재작성, `git reset --hard` 등 복구하기 어려운 Git 명령을 사용하지 않는다.
- 한 커밋에는 하나의 논리적인 변경 목적만 포함한다.
- 커밋 메시지는 `영문 접두사 : 한국어 내용` 형식으로 작성한다.
- 접두사는 소문자로 작성하고 콜론 앞뒤에 공백을 하나씩 둔다.

권장 접두사는 다음과 같다.

- `feat` : 새로운 기능 추가
- `fix` : 오류 수정
- `refactor` : 동작 변경 없는 코드 구조 개선
- `test` : 테스트 추가 또는 수정
- `docs` : 문서 변경
- `chore` : 유지보수, 도구 및 기타 작업
- `build` : 빌드 또는 의존성 구성 변경
- `ci` : CI 구성 변경
- `perf` : 성능 개선
- `style` : 코드 동작에 영향을 주지 않는 서식 변경
- `revert` : 이전 변경 되돌리기

예시:

```text
feat : 카드사별 상태 필터 추가
fix : 취소 거래의 음수 금액 검증 오류 해결
refactor : 엑셀 행 배치 계산 로직 정리
docs : 개발 및 테스트 절차 정리
chore : 개발 도구 설정 정리
```
